<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.registropersonas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegistroPersonas">
        <activity android:name=".vista.SeleccionAccion"/>
        <activity android:name=".vista.ActualizaRegistro"/>
        <activity android:name=".vista.ConsultaRegistro"/>
        <activity android:name=".vista.EliminaRegistro"/>
        <activity android:name=".vista.RegistroCredenciales"/>
        <activity android:name=".vista.CreaRegistro"/>
        <activity android:name=".vista.RestableceContrasena"/>
        <activity android:name=".vista.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>